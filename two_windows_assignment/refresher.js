var num1, num2;

num1 = window.prompt('input first integer', '0');

num2 = window.prompt("input second integer", "0");

if(parseInt(num1,10) > parseint(num2,10))

    {
    console.log("the larger of " + num1 + " and " + num2+ " is " + num2);
    }
    else
    {
    console.log("values for" + num1 + "and" + num2 + "are equal");

    }
