import React, { Component } from 'react';

const Box3Bottom = (props) => {
  return(
    <div className="Box3Bottom">
      <button className="folder-label">Folder Label</button>
    </div>
  )
}

export default Box3Bottom
